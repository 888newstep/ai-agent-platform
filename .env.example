# AI Agent Platform - Environment Variables Example
# Copy this file to .env and fill in the values you actually use.

# ============================================
# Default model provider (application.yml uses DeepSeek by default)
# ============================================
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# Optional alternative model providers
QIANWEN_API_KEY=your-qianwen-api-key-here
DOUBAO_API_KEY=your-doubao-api-key-here
QWEN3_FLASH_API_KEY=your-qwen3-flash-api-key-here

# Embedding provider (application.yml uses SiliconFlow by default)
SILICONFLOW_API_KEY=your-siliconflow-api-key-here

# ============================================
# Public API protection
# Fixed-window Redis quotas; fail-open keeps local development available if Redis is down.
# ============================================
AI_RATE_LIMIT_ENABLED=true
AI_RATE_LIMIT_REQUESTS_PER_MINUTE=30
AI_RATE_LIMIT_FAIL_OPEN=true
AI_COST_BUDGET_ENABLED=true
AI_ESTIMATED_TOKENS_PER_MINUTE=12000
AI_MAX_ESTIMATED_TOKENS_PER_REQUEST=4000
AI_MAX_INPUT_CHARACTERS=12000
AI_TOKENS_PER_CHARACTER=1.0
AI_PROMPT_OVERHEAD_TOKENS=256
AI_COST_BUDGET_FAIL_OPEN=true
# ============================================
# Security
# ============================================
JWT_SECRET=replace-with-a-random-secret-at-least-32-characters-long
ADMIN_API_KEY=replace-with-a-random-admin-key
# CORS: keep the allowlist explicit; add frontend origins when needed.
AI_CORS_ALLOWED_ORIGINS=http://localhost:8081
AI_CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
AI_CORS_ALLOWED_HEADERS=Authorization,Content-Type,Accept,X-Admin-Api-Key,X-Requested-With
AI_CORS_ALLOW_CREDENTIALS=false
AI_CORS_MAX_AGE=3600

# Local Compose monitoring needs public metrics; set false in production.
AI_ACTUATOR_PUBLIC_METRICS=true
AI_ACTUATOR_HEALTH_DETAILS=always
AI_ACTUATOR_EXPOSED_ENDPOINTS=health,info,metrics,prometheus

# ============================================
# Tool outbound allowlist
# Wildcards like *.example.com are supported.
# Private / loopback hosts are rejected by ToolService.
# ============================================
AI_TOOL_ALLOWED_HOSTS=api.example.com,*.example.com
AI_TOOL_DATABASE_QUERY_ENABLED=true
AI_TOOL_DATABASE_MAX_ROWS=100
AI_TOOL_DATABASE_QUERY_TIMEOUT_SECONDS=5
AI_TOOL_API_CALL_ENABLED=true
AI_TOOL_API_TIMEOUT_MS=30000
AI_TOOL_ALLOWED_METHODS=GET,POST
AI_TOOL_MAX_REQUEST_CHARS=8000
AI_TOOL_MAX_RESPONSE_CHARS=8000

# ============================================
# MySQL
# ============================================
# MySQL and Redis run on the local Windows host in the hybrid setup.
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=ai_agent
MYSQL_USERNAME=root
MYSQL_PASSWORD=replace-with-a-strong-mysql-password
MYSQL_POOL_MAX_SIZE=50
MYSQL_POOL_MIN_IDLE=10
MYSQL_CONNECTION_TIMEOUT_MS=30000
MYSQL_IDLE_TIMEOUT_MS=600000
MYSQL_MAX_LIFETIME_MS=1800000
# Optional override; normally you do not need to change this.
HIBERNATE_DIALECT=org.hibernate.dialect.MySQLDialect

# ============================================
# Redis
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=replace-with-a-strong-redis-password
# Redis logical database allocation: newagent = DB 1.
REDIS_DATABASE=1
REDIS_POOL_MAX_ACTIVE=100
REDIS_POOL_MAX_IDLE=50
REDIS_POOL_MIN_IDLE=10
REDIS_POOL_MAX_WAIT=2000ms
REDIS_TIMEOUT=3000ms

# ============================================
# Milvus
# ============================================
# Point these values to the cloud Milvus service in the hybrid setup.
MILVUS_HOST=cloud-milvus-host
MILVUS_PORT=19530
MILVUS_DATABASE_NAME=cs_agent
MILVUS_COLLECTION_NAME=ai_agent_documents
AI_VECTOR_STORE_MODE=langchain
MILVUS_READ_ONLY=false
MILVUS_CONNECTION_TIMEOUT_MS=2000

# ============================================
# Optional RabbitMQ connectivity preflight only
# newagent does not currently depend on RabbitMQ at runtime.
# ============================================
RABBITMQ_HOST=cloud-rabbitmq-host
RABBITMQ_PORT=5672

# ============================================
# Observability (used by docker-compose monitoring stack)
# ============================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=replace-with-a-strong-grafana-password

# ============================================
# Optional data import feature
# ============================================
# Versioned sample data; point this to a private directory for real evaluation data.
AI_EVALUATION_DATASET_DIRECTORY=./examples/evaluation-datasets
AI_EVALUATION_REPORT_DIRECTORY=./evaluation-reports

# Optional production cross-encoder reranker. Default remains embedding.
AI_RAG_RERANK_PROVIDER=embedding
AI_RAG_RERANK_ENDPOINT=https://api.siliconflow.cn/v1/rerank
AI_RAG_RERANK_API_KEY=
AI_RAG_RERANK_MODEL=BAAI/bge-reranker-v2-m3
AI_RAG_RERANK_MIN_SCORE=0.55
AI_RAG_RERANK_TIMEOUT_MS=10000
AI_RAG_RERANK_MAX_DOCUMENT_CHARACTERS=4000
AI_RAG_RERANK_FALLBACK_TO_EMBEDDING=false

CS_IMPORT_DATA_DIR=
CS_MINIMUM_VERIFICATION_LEVEL=HIGH
CS_HANDOFF_MESSAGE=当前知识库没有足够信息可以可靠回答，建议转人工客服进一步处理。
CS_MAX_EVIDENCE=5
